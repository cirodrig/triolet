
# Directories
BUILDDIR=build/program
SRCDIR=@srcdir@/src/program

prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@

## System-dependent programs

CC=@CC@
HC=ghc
HSC2HS=hsc2hs
ESED=@cir_esed@

## System-dependent flags

# Compiler and linker flags
ifeq (@BUILD_32BIT@, yes)

HSC2HS_32BIT_FLAG=--cflag=-m32 --lflag=-m32
CC_32BIT_FLAG=-m32
L_32BIT_FLAG=-optl-m32

endif

HSC2HSFLAGS=$(HSC2HS_32BIT_FLAG)
HCFLAGS=
CCFLAGS=$(CC_32BIT_FLAG)
LFLAGS=$(L_32BIT_FLAG)

# Include directories
INCLUDEDIRS=@cir_hs_libdir@/include \
	@cir_python_incdir@

# Library directories
LIBDIRS=

# Haskell package names
PACKAGES=base containers mtl fgl language-python-pyon gluon

# Library names
LIBS=python@cir_python_version@
