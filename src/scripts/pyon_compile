import sys
import os.path
import pyon.tests.run_tests

if len(sys.argv) != 2:
    print "Usage:", os.path.basename(sys.argv[0]), "<filename>"
    sys.exit(1)

filename = sys.argv[1]
pyon.tests.run_tests.tryCompile(filename, show_traceback=True)
