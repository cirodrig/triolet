
// The info table for stream objects
extern pointer pyon.internal.stream.Stream_info;

// Stream constructors
extern owned pyon.internal.stream.Stream_return;
extern owned pyon.internal.stream.Stream_bind;
