
import procedure free_pap (pointer) -> ();
import procedure apply_o_f (owned, owned) -> owned;
import procedure apply_o (owned, owned, pointer) -> ();
import procedure apply_i32_f (owned, uint32) -> owned;
import procedure apply_i32 (owned, uint32, pointer) -> ();
import procedure apply_f32_f (owned, float) -> owned;
import procedure apply_f32 (owned, float, pointer) -> ();
