
extern procedure core.internal.lazy.force_Lazy (pointer, uint, uint) -> ();

extern procedure core.internal.lazy.force_Lazy_owned (pointer) -> owned;

// extern procedure core.internal.lazy.make_Lazy_header (SA, pointer) -> owned;
