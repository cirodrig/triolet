Triolet is a high-level programming language with a syntax like
Python.  It compiles to parallel code that often rivals C in
performance.

Building
--------

This is a Cabal package.  The typical build process is

ghc --make Setup
./Setup configure
./Setup build
./Setup install

